import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.electricecoedge.app',
  appName: 'ElectricEcoEdge',
  webDir: 'build'
};

export default config;
